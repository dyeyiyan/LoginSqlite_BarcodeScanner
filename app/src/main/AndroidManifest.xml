<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.deviyan.loginusingsqlitewithbarcodescanner">

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/user_pic"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.IOTPracticalExam">
        <activity
            android:name="com.deviyan.loginusingsqlitewithbarcodescanner.MainActivity"
            android:exported="false" />
        <activity
            android:name="com.deviyan.loginusingsqlitewithbarcodescanner.Login"
            android:exported="false" />
        <activity
            android:name="com.deviyan.loginusingsqlitewithbarcodescanner.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>